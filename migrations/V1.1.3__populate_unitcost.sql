UPDATE ASG.NWT.PRODUCT
SET UNITCOST = (
    SELECT PF."UnitCost"
    FROM ASG.NWT.PRODUCT_FRESH PF
    WHERE "PRODUCT"."productID" = PF."Product ID"
)
WHERE EXISTS (
    SELECT 1
    FROM ASG.NWT.PRODUCT_FRESH PF
    WHERE "PRODUCT"."productID" = PF."Product ID"
);
